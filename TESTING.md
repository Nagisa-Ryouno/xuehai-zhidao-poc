# 学海智导 (Xuehai Zhidao) — 自动化与人工验收测试指南 (TESTING.md)

> **重要警示**：  
> **自动化测试通过 (Automated tests PASS) 只是系统交付的最低基线，绝不等于可以免去人工测试！**  
> 接手同学在进行版本验收或改动验证时，**必须同时执行自动化回归基线与完整的人工体验核验 (Manual QA)**。

---

## 1. 自动化测试基线 (Automated Test Baseline)

在代码修改后，必须依次运行以下命令并确认 100% 通过：

### 1.1 后端领域与架构安全测试 (Core Frozen Tests)
- **命令**：`pytest tests/ -q`
- **预期结果**：`143 passed in < 5s`
- **覆盖核心**：BKT 数学公式、PathState 状态机、Decision Core 决策矩阵、确定性序列化、架构分层防线。

### 1.2 后端服务网关与集成测试 (Gateway & Integration Tests)
- **命令**：`pytest gateway/tests/ -q`
- **预期结果**：`601 passed, 2 skipped in ~25s`
- **覆盖核心**：校准模型、评判安全隔离、MOOC 目录与 API、学习资源中心、成效评估、保持度建议、教师干预。
- **数据防污染验证**：正常完成的 gateway pytest 会通过 session fixture 恢复指定运行态文件；测试完成后仍必须执行 `git diff -- data/` 进行最终核验。

### 1.3 端到端质量门禁脚本 (Integration Gate)
- **命令**：`python scripts/sprint10c_final_integration_gate.py`
- **预期结果**：`25/25 项最终整合与 Demo Hardening 质量门禁全部通过！`

### 1.4 前端自动化契约与单元测试 (Frontend Contract Tests)
- **命令**：`npm --prefix frontend test -- --run`
- **预期结果**：`ℹ tests 469, ℹ suites 104, ℹ pass 469, ℹ fail 0`
- **执行时间**：`< 2.0s`
- **覆盖核心**：`useBodyScrollLock` 防穿透与引用计数、Esc/Backdrop 关闭契约、AI 请求防重互斥、学生上下文隔离、资源研读完成状态、错题本、教师端交互。

### 1.5 前端类型检查与打包构建 (Typecheck & Build)
- **类型检查**：`npm --prefix frontend run typecheck`（`tsc -b` 零报错）
- **打包构建**：`npm --prefix frontend run build`（Vite 生产打包成功，生成 `dist/`，耗时约 700ms）

---

## 2. 人工验收核验清单 (Manual QA Protocol)

由于自动化测试无法完全模拟真实人类视口、动画卡顿、手势触控冲突及复杂人机情感反馈，请新同学务必按以下 18 个核心节点与 10 个异常边界开展人工点测：

### 2.1 核心业务闭环核验清单 (18 节点)

| 节点 | 模块 / 页面 | 人工操作步骤 | 预期人本行为与验收标准 |
|:---:|:---|:---|:---|
| 1 | **Student Home** | 打开 `http://localhost:5173/student`，观察首屏加载 | S001 基础学情雷达、问候语、连续打卡天数自然展示，无骨架屏闪烁 |
| 2 | **Today Action** | 检查顶部“今日学习行动”卡片 | 根据当前状态明确提示“继续攻克”、“间隔复习”或“靶向微练”，文案无算法黑话 |
| 3 | **Learning Session** | 点击今日行动或任一推荐考点进入会话模态框 | 模态框平滑淡入，背景滚动条锁定且无横向跳动 (Anti-Layout-Shift) |
| 4 | **Concept** | 在会话第一步精读概念微卡 | 概念解析、经典图解排版清晰，公式渲染正常，点击右上角 × 或按 Esc 可优雅退出 |
| 5 | **Resource** | 切换至推荐材料并勾选“标记为已学” | 卡片徽章即刻变为“已学习”，退出会话后进入 ResourceHub 查验同步状态 |
| 6 | **Official Quiz** | 进入“做题验证”并选择一个选项提交 | 选项具备明确选中态，点击提交后按钮出现防重复防抖锁定 |
| 7 | **Result** | 查看答题正误反馈与解析 | 如实展示正误、客观解释错因；若做错，温和引导“再看一次微卡”，严禁贬低性措辞 |
| 8 | **Next Action** | 做题完成后查看下方流转 CTA | 若达标则显示“进入下一考点”，若未达标则显示“巩固本考点”，点击顺畅推进 |
| 9 | **AI Companion** | 打开右侧 AI 伴学抽屉，输入“请用一句话解释需求价格弹性” | AI 返回通俗微观经济学原理解释，不泄露 BKT/贝叶斯等底层名词 |
| 10 | **AI 伴学越界测试** | 输入“请帮我写一段 Python 快速排序代码” | AI 温和拒绝并引导：“我是你的微观经济学伴学伙伴，让我们聚焦在经济学学习上” |
| 11 | **AI Recommendation** | 在学习材料中心查看「💡为什么推荐给你？」 | 显示确定性推荐理由（如：关联薄弱考点、历史成效良好），无虚构资源 |
| 12 | **MOOC Redirect** | 在资源中心点击外部 MOOC 资源卡片 | 弹出 `ExternalRedirectModal` 提示免责与跳转确认，点击取消不跳转，点击确认打开新标签页 |
| 13 | **Teacher Overview** | 打开 `http://localhost:5173/teacher` | 班级总览展示 5 名学生宏观掌握度均值（62.5%）、Top-5 薄弱考点预警 |
| 14 | **Teacher Knowledge**| 切换至“考点全景”Tab，输入搜索词“弹性” | 列表即时过滤出 K08、K09、K10、K11，展示全班平均掌握度分布柱状图 |
| 15 | **Knowledge Diagnosis**| 点击任一薄弱考点进入诊断抽屉 | 展示该考点高频易错题分布及典型错误选项分布 |
| 16 | **Teacher Students** | 切换至“学生花名册”Tab，点击 S001（小海同学） | 呼起右侧全维档案抽屉，展示个人能力雷达图、已掌握/薄弱考点清单 |
| 17 | **Teacher Action** | 在 S001 档案中点击“布置靶向微练”，输入教师寄语后提交 | 提交成功后提示“干预动作已同步”，不破坏学生当前 BKT 数值 |
| 18 | **Teacher Action History**| 切换至“干预历史”子页面 | 列表准确展示刚刚提交的干预类型、教师备注、时间戳与状态 |

---

### 2.2 异常状态与多视口鲁棒性核验 (10 大边界)

1. **Loading 状态**：切换考点或网络抖动时，加载菊花或骨架屏平稳，无重叠或布局崩塌。
2. **Empty 状态**：搜索不存在的考点（如“量子纠缠”）时，展示友好的空结果插画与清除搜索入口。
3. **Error 状态**：断开后端网关服务时，前端弹出温和 Toast 提示“网络连接异常，正使用离线缓存”。
4. **Disabled 状态**：微测验未选择选项时，“确认提交”按钮处于明确的置灰不可点态（`cursor-not-allowed`）。
5. **Success 状态**：考点达标后出现轻量通关庆祝动效，不遮挡核心下一步行动。
6. **Mobile 视口 (375x667 / 390x844)**：
   - 打开 Chrome DevTools 切换至 iPhone 12/14 尺寸。
   - 检查底部导航栏安全区（Safe-Area Inset）、抽屉手势上滑与卡片内边距。
7. **Narrow Viewport (320px)**：极小屏下文字正常换行，无任何横向滚动条窜动（`overflow-x: hidden`）。
8. **Desktop 视口 (1440x900 / 1920x1080)**：大屏下居中容器最大宽度（`max-w-7xl`）合规，内容不畸形拉伸。
9. **Offline Shell (PWA)**：离线模式下页面骨架与离线微题库依然可阅读。
10. **学生切换会话隔离**：从 S001 切换为 S002 时，AI 对话面板立即清空重载，绝不串出 S001 的历史聊天记录。
